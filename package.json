{
  "name": "deepsel-cms-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "admin",
    "themes/*",
    "packages/*",
    "tests/e2e"
  ],
  "scripts": {
    "build": "npm run build --workspace=client",
    "dev": "npm run dev --workspace=client",
    "format": "npm run format --workspace=client && npm run format --workspace=admin && npm run format --workspace=packages/cms-react && npm run format --workspace=packages/cms-utils",
    "format:check": "npm run format:check --workspace=client && npm run format:check --workspace=admin && npm run format:check --workspace=packages/cms-react && npm run format:check --workspace=packages/cms-utils",
    "prepush": "npm run prepush:backend && npm run prepush:packages && npm run prepush:admin && npm run e2e",
    "prepush:backend": "cd backend && make prepush",
    "prepush:packages": "npm run prepush --workspace=packages/cms-utils --workspace=packages/cms-react",
    "prepush:admin": "npm run test --workspace=admin && npm run build:lib --workspace=admin",
    "build:admin": "npm run build --workspace=admin",
    "e2e": "npm run test --workspace=tests/e2e"
  },
  "overrides": {
    "@mui/material": {
      "react": "$react",
      "react-dom": "$react-dom"
    },
    "@mui/x-data-grid": {
      "react": "$react",
      "react-dom": "$react-dom"
    }
  },
  "devDependencies": {
    "happy-dom": "^20.0.11",
    "prettier": "^3.8.1",
    "prettier-plugin-astro": "^0.14.1"
  }
}
