name: Packages CI/CD

on:
  pull_request:
    paths:
      - 'packages/**'
      - 'admin/**'
  push:
    branches:
      - main
    paths:
      - 'packages/**'
      - 'admin/**'
    tags:
      - 'cms-utils-v*'
      - 'cms-react-v*'
      - 'cms-editor-v*'
      - 'admin-v*'

jobs:
  # Detect which packages changed
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      cms-utils: ${{ steps.filter.outputs.cms-utils }}
      cms-react: ${{ steps.filter.outputs.cms-react }}
      cms-editor: ${{ steps.filter.outputs.cms-editor }}
      admin: ${{ steps.filter.outputs.admin }}
      # For tag events, detect which package the tag is for
      tagged-package: ${{ steps.tag-check.outputs.package }}
      tag-version: ${{ steps.tag-check.outputs.version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check tag
        id: tag-check
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          if [[ "$TAG" == cms-utils-v* ]]; then
            echo "package=cms-utils" >> $GITHUB_OUTPUT
            echo "version=${TAG#cms-utils-v}" >> $GITHUB_OUTPUT
          elif [[ "$TAG" == cms-react-v* ]]; then
            echo "package=cms-react" >> $GITHUB_OUTPUT
            echo "version=${TAG#cms-react-v}" >> $GITHUB_OUTPUT
          elif [[ "$TAG" == cms-editor-v* ]]; then
            echo "package=cms-editor" >> $GITHUB_OUTPUT
            echo "version=${TAG#cms-editor-v}" >> $GITHUB_OUTPUT
          elif [[ "$TAG" == admin-v* ]]; then
            echo "package=admin" >> $GITHUB_OUTPUT
            echo "version=${TAG#admin-v}" >> $GITHUB_OUTPUT
          fi

      - name: Detect changed packages
        uses: dorny/paths-filter@v3
        id: filter
        if: "!startsWith(github.ref, 'refs/tags/')"
        with:
          filters: |
            cms-utils:
              - 'packages/cms-utils/**'
            cms-react:
              - 'packages/cms-react/**'
            cms-editor:
              - 'packages/cms-editor/**'
            admin:
              - 'admin/**'

  # cms-utils CI
  cms-utils:
    needs: detect-changes
    if: needs.detect-changes.outputs.cms-utils == 'true' || needs.detect-changes.outputs.tagged-package == 'cms-utils'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test --workspace=packages/cms-utils

      - name: Lint
        run: npm run lint --workspace=packages/cms-utils

      - name: Check formatting
        run: npm run format:check --workspace=packages/cms-utils

      - name: Build
        run: npm run build --workspace=packages/cms-utils

      - name: Update npm
        if: needs.detect-changes.outputs.tagged-package == 'cms-utils'
        run: npm install -g npm@latest

      - name: Publish package
        if: needs.detect-changes.outputs.tagged-package == 'cms-utils'
        run: npm publish --provenance --workspace=packages/cms-utils

  # cms-react CI
  cms-react:
    needs: detect-changes
    if: needs.detect-changes.outputs.cms-react == 'true' || needs.detect-changes.outputs.tagged-package == 'cms-react'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Build cms-utils (dependency)
        run: npm run build --workspace=packages/cms-utils

      - name: Run tests
        run: npm test --workspace=packages/cms-react

      - name: Lint
        run: npm run lint --workspace=packages/cms-react

      - name: Check formatting
        run: npm run format:check --workspace=packages/cms-react

      - name: Build
        run: npm run build --workspace=packages/cms-react

      - name: Update npm
        if: needs.detect-changes.outputs.tagged-package == 'cms-react'
        run: npm install -g npm@latest

      - name: Publish package
        if: needs.detect-changes.outputs.tagged-package == 'cms-react'
        run: npm publish --provenance --workspace=packages/cms-react

  # cms-editor CI
  cms-editor:
    needs: detect-changes
    if: needs.detect-changes.outputs.cms-editor == 'true' || needs.detect-changes.outputs.tagged-package == 'cms-editor'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Build dependencies
        run: |
          npm run build --workspace=packages/cms-utils
          npm run build --workspace=packages/cms-react

      - name: Run tests
        run: npm test --workspace=packages/cms-editor

      - name: Lint
        run: npm run lint --workspace=packages/cms-editor

      - name: Check formatting
        run: npm run format:check --workspace=packages/cms-editor

      - name: Build
        run: npm run build --workspace=packages/cms-editor

      - name: Update npm
        if: needs.detect-changes.outputs.tagged-package == 'cms-editor'
        run: npm install -g npm@latest

      - name: Publish package
        if: needs.detect-changes.outputs.tagged-package == 'cms-editor'
        run: npm publish --provenance --workspace=packages/cms-editor

  # admin CI
  admin:
    needs: detect-changes
    if: needs.detect-changes.outputs.admin == 'true' || needs.detect-changes.outputs.tagged-package == 'admin'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Build workspace dependencies
        run: |
          npm run build --workspace=packages/cms-utils
          npm run build --workspace=packages/cms-react
          npm run build --workspace=packages/cms-editor

      - name: Build library
        run: npm run build:lib --workspace=admin

      - name: Update npm
        if: needs.detect-changes.outputs.tagged-package == 'admin'
        run: npm install -g npm@latest

      - name: Publish package
        if: needs.detect-changes.outputs.tagged-package == 'admin'
        run: npm publish --provenance --workspace=admin
